apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "faucet.fullname" . }}-authenticator
  labels:
    {{- include "faucet.labels" . | nindent 4 }}
data:
  env: |
    {{- .Values.config.authenticator.env | nindent 4 }}